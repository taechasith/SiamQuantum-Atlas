services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: siamquantum
      POSTGRES_PASSWORD: siamquantum
      POSTGRES_DB: siamquantum_atlas
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
